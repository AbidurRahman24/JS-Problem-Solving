// const num = 6;
// const result = Math.round(Math.random(num)*10)
// console.log(result);



for(var i = 0; i<10; i++){
    var random = Math.random();
    var floored = Math.round(random*100);
    console.log(floored);
}